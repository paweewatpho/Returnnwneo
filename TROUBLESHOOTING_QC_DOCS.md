# คู่มือการตรวจสอบและแก้ไขปัญหา: ขั้นตอน QC ไม่มีข้อมูลมาแสดงใน Docs

## 📋 สรุปปัญหา
ไม่มีข้อมูลแสดงใน **Step 5: ส่งเอกสารคืน (Docs)** หลังจากทำการตรวจสอบคุณภาพ (QC) ใน **Step 4**

## 🔍 การวิเคราะห์ปัญหา

### โครงสร้างการไหลของข้อมูล (Data Flow)

```
Step 3: รับสินค้าเข้า Hub
    ↓ (status = 'NCR_HubReceived')
Step 4: ตรวจสอบคุณภาพ (QC)
    ↓ (status = 'NCR_QCCompleted')
Step 5: ส่งเอกสารคืน (Docs) ← ไม่มีข้อมูล!
```

### เงื่อนไขการกรองข้อมูล

**Step 4 (QC) - กรองข้อมูลเข้า:**
```typescript
// Step4HubQC.tsx (บรรทัด 41-48)
const receivedItems = items.filter(item => {
    const isReceived = item.status === 'NCR_HubReceived' || item.status === 'ReceivedAtHub';
    const isNCR = item.documentType === 'NCR' || !!item.ncrNumber || item.status === 'NCR_HubReceived';
    return isReceived && isNCR;
});
```

**Step 4 (QC) - อัปเดต status เมื่อเสร็จสิ้น:**
```typescript
// Step4HubQC.tsx (บรรทัด 116)
const updates = {
    status: 'NCR_QCCompleted',  // ✅ อัปเดตเป็น NCR_QCCompleted
    condition: qcSelectedItem.condition,
    disposition: selectedDisposition,
    ...
};
```

**Step 5 (Docs) - กรองข้อมูลเข้า:**
```typescript
// Step5HubDocs.tsx (บรรทัด 29)
const processedItems = items.filter(item => 
    item.status === 'NCR_QCCompleted' || item.status === 'QCCompleted'  // ✅ รองรับทั้งสอง status
);
```

## ✅ สรุป: โค้ดถูกต้อง!

จากการตรวจสอบ พบว่า**โค้ดทำงานถูกต้อง** และไม่มีปัญหาในการเชื่อมต่อระหว่าง Step 4 และ Step 5

## 🎯 สาเหตุที่เป็นไปได้

### 1. ยังไม่มีข้อมูลที่ผ่าน QC จริงๆ
- ยังไม่ได้สร้าง NCR
- NCR ยังไม่ผ่าน Step 2 (Logistics)
- NCR ยังไม่ผ่าน Step 3 (Receive)
- NCR ยังไม่ได้ทำการ QC ใน Step 4

### 2. ข้อมูลเป็น Collection (COL) ไม่ใช่ NCR
Collection items จะข้าม Step 4 (QC) และไปที่ Step 7 (Docs) แทน

### 3. ข้อมูลไม่มี Disposition
รายการที่ผ่าน QC แต่ไม่มี `disposition` จะถูกแก้ไขเป็น `'InternalUse'` อัตโนมัติ

## 🛠️ วิธีการตรวจสอบ

### ขั้นตอนที่ 1: เปิด Debug Panel

1. เปิดระบบ **Return Operations Hub**
2. ดูที่มุมขวาล่างของหน้าจอ
3. จะเห็น **Debug Panel** แสดงข้อมูลดังนี้:

```
🔍 Data Flow Inspector
├─ Step 4: QC Queue          → จำนวนรายการรอ QC
├─ Step 5: Docs Queue         → จำนวนรายการที่ผ่าน QC
├─ ⚠️ ข้อมูลไม่สมบูรณ์        → รายการที่ไม่มี Disposition
└─ 📊 สรุป NCR Items          → สรุปข้อมูลทั้งหมด
```

### ขั้นตอนที่ 2: ตรวจสอบข้อมูล

**กรณีที่ 1: ไม่มีข้อมูลทั้ง Step 4 และ Step 5**
```
💡 คำแนะนำ
ไม่พบข้อมูลในทั้ง Step 4 และ Step 5
กรุณาตรวจสอบ:
1. มีการสร้าง NCR แล้วหรือไม่?
2. NCR ผ่าน Step 2 (Logistics) แล้วหรือไม่?
3. NCR ผ่าน Step 3 (Receive) แล้วหรือไม่?
```

**กรณีที่ 2: มีข้อมูลใน Step 4 แต่ไม่มีใน Step 5**
```
💡 คำแนะนำ
มีรายการรอตรวจสอบใน Step 4
กรุณาไปที่ Step 4 เพื่อทำการตรวจสอบคุณภาพ (QC)
```

**กรณีที่ 3: มีข้อมูลใน Step 5 แล้ว**
```
✅ Step 5: Docs Queue
รายการที่ผ่าน QC แล้ว: X รายการ
```

## 📝 วิธีการทดสอบ

### ทดสอบการไหลของข้อมูลแบบ End-to-End

1. **สร้าง NCR ใหม่** (Step 1)
   - ไปที่ "1. แจ้งคืนสินค้า"
   - กรอกข้อมูลและเพิ่มสินค้า
   - กด "บันทึกคำขอคืนสินค้า"

2. **ระบุขนส่ง** (Step 2)
   - ไปที่ "2. รวบรวมและระบุขนส่ง"
   - เลือกรายการที่สร้าง
   - ระบุข้อมูลขนส่งและยืนยัน
   - ✅ Status จะเป็น `'NCR_InTransit'`

3. **รับสินค้าเข้า Hub** (Step 3)
   - ไปที่ "3. รับสินค้าเข้า Hub"
   - เลือกรายการและกด "รับสินค้า"
   - ✅ Status จะเป็น `'NCR_HubReceived'`

4. **ตรวจสอบคุณภาพ** (Step 4)
   - ไปที่ "4. ตรวจสอบคุณภาพ (QC)"
   - เลือกรายการจากคิวด้านซ้าย
   - เลือก Condition (สภาพสินค้า)
   - เลือก Disposition (การตัดสินใจ)
   - กด "ยืนยันผลการตรวจสอบ"
   - ✅ Status จะเป็น `'NCR_QCCompleted'`

5. **ตรวจสอบใน Docs** (Step 5)
   - ไปที่ "5. ส่งเอกสารคืน (Docs)"
   - ✅ ควรเห็นรายการที่ผ่าน QC แล้ว

## 🔧 การแก้ไขปัญหา

### ปัญหา: ข้อมูลไม่มี Disposition

**อาการ:**
```
⚠️ ข้อมูลไม่สมบูรณ์
พบ X รายการที่ไม่มี Disposition
```

**วิธีแก้:**
1. กลับไปที่ Step 4 (QC)
2. เลือกรายการที่มีปัญหา
3. เลือก Disposition ใหม่
4. กด "ยืนยันผลการตรวจสอบ" อีกครั้ง

### ปัญหา: Status ไม่ถูกต้อง

**วิธีตรวจสอบใน Firebase:**
1. เปิด Firebase Console
2. ไปที่ Realtime Database
3. เปิด `return_records`
4. ตรวจสอบ `status` ของแต่ละรายการ

**Status ที่ถูกต้อง:**
- Step 3 → Step 4: `'NCR_HubReceived'`
- Step 4 → Step 5: `'NCR_QCCompleted'`

## 📊 ตัวอย่างข้อมูลที่ถูกต้อง

```json
{
  "id": "NCR-2025-0001",
  "status": "NCR_QCCompleted",
  "condition": "New",
  "disposition": "RTV",
  "destinationCustomer": "สาย 3",
  "documentType": "NCR",
  "ncrNumber": "NCR-2025-0001",
  ...
}
```

## 🎨 การลบ Debug Component (Production)

เมื่อแก้ไขปัญหาเสร็จแล้ว ให้ลบ Debug Component ออก:

**ไฟล์:** `components/Operations.tsx`

```tsx
// ลบบรรทัดนี้ออก:
import { DebugDataFlow } from './operations/components/DebugDataFlow';

// และลบบรรทัดนี้ออก:
{/* Debug Component - Remove in production */}
<DebugDataFlow />
```

## 📞 ติดต่อสอบถาม

หากยังพบปัญหา กรุณาติดต่อทีมพัฒนา พร้อมแนบข้อมูลจาก Debug Panel
